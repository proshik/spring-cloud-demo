spring:
  application:
    name: monitoring
  cloud:
    config:
      uri: http://config:8888
      password: password
      username: user
      fail-fast: true
      retry:
        multiplier: 1.5
        initial-interval: 2000
        max-interval: 10000
