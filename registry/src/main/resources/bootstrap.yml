spring:
  application:
    name: registry
  cloud:
    config:
      uri: http://config:8888
      fail-fast: true
      password: password
      username: user
      retry:
        initial-interval: 2000
        max-attempts: 6
