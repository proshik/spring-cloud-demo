spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/proshik/config-repo
          username: {GITHUB_USERNAME}
          password: {GITHUB_PASSWORD}
          search-paths: spring-cloud-demo

server:
  port: 8888

security:
  user:
    password: password

logging:
  level:
    org.springframework: DEBUG